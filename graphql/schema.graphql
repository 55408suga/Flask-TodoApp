type User {
  id: ID!
  username: String!
}

type Todo {
  id: ID!
  name: String!
  is_done: Boolean!
  deadline: String
  user: User!
}

type Query {
  hello: String!
  todos: [Todo!]!
  todo(id: ID!): Todo
}
